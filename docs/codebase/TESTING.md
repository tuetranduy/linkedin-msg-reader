# TESTING.md — Test Setup & Strategy

> Generated by `acquire-codebase-knowledge` skill (v1.3) on 2026-05-24

## Current State

**No automated test suite is configured for this project.**

Neither `package.json` (frontend) nor `server/package.json` (backend) defines a `test` script, and no test runner framework packages (`jest`, `vitest`, `mocha`, `@testing-library/react`, `supertest`, etc.) appear in any `dependencies` or `devDependencies`.

> Evidence: root `package.json`, `server/package.json` — no test script, no test framework

## Playwright (Manual / Exploratory)

`.playwright-mcp/` at the repo root contains Playwright browser automation session artifacts (screenshots, page snapshots, console logs). These are not automated tests — they are artifacts from interactive Playwright MCP tool sessions.

- Evidence: `.playwright-mcp/` directory contents (`.yml` snapshots, `.png` screenshots, `.log` console logs)

## Test Runner

- **[TODO]** No test runner is configured. `vitest` (Vite-native, works alongside Vite config) or `jest` would be the natural choices for this stack.

## Test File Organization

- **[TODO]** No `__tests__/`, `tests/`, or `*.spec.ts` / `*.test.ts` files exist in the repository.

## Assertion Library

- **[TODO]** None configured.

## Mocking Strategy

- **[TODO]** No mocking strategy established.

## Integration vs Unit Tests

- **[TODO]** No integration tests exist. The MongoDB layer could be tested with `mongodb-memory-server` for in-memory isolation.

## Coverage

- **[TODO]** No coverage threshold configured.

## Recommendations (for future implementation)

| Area                  | Suggested Tool                                    |
|-----------------------|---------------------------------------------------|
| Frontend unit tests   | `vitest` + `@testing-library/react`               |
| Frontend component tests | `@testing-library/react` + `msw` for API mocks |
| Backend unit tests    | `vitest` or `jest` + `mongodb-memory-server`      |
| Backend API tests     | `supertest` against Express app                   |
| E2E tests             | Playwright (framework already present via MCP)    |

[ASK USER] Is adding a test suite in scope? If so, which testing approach is preferred: unit tests first, or E2E tests?
